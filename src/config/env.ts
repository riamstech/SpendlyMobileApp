export const config = {
  // TODO: Update this to match your .env or backend URL for mobile
  // Example: 'https://api.spendly.money/api'
  apiBaseUrl: 'https://api.spendly.money/api',
  apiTimeout: 30000,
  // Storage URL for serving uploaded files (avatars, receipts, etc.)
  // Remove /api suffix to get base URL
  storageUrl: 'https://api.spendly.money',
};


